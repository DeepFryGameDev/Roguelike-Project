// Purpose: Player's class - all of them have a different play style and stats
// Directions: Attach to Player GameObject
// Other notes: This should attach to the player's GameObject based on class chosen when starting a new game - to be implemented

public class BaseArcher : BasePlayer
{
    protected override void Awake()
    {
        base.Awake();
    }

    // Update is called once per frame
    void Update()
    {

    }
}